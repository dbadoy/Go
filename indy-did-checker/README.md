# Usage
```
Set library Indy-vdr

$ git clone https://github.com/hyperledger/indy-vdr.git
$ cd Indy-vdr

// Install Rust
$ curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
$ cargo build --lib


$ cp /home/[$USER]/indy-vdr/target/release/deps/libindy_vdr.* /use/local/lib
```

